# Pervis PRO 一键安装环境指南

## 🚀 快速开始

### 第一步：检查当前环境
```powershell
# 在项目根目录运行
.\环境检查.ps1
```

### 第二步：安装缺失组件

根据检查结果，安装以下必需组件：

## 📦 必需组件安装

### 1. Python 3.10+
- **下载地址**: https://www.python.org/downloads/
- **安装要点**: 
  - ✅ 勾选 "Add Python to PATH"
  - ✅ 勾选 "Install for all users"
  - ✅ 选择 "Customize installation" → 勾选所有选项

### 2. Node.js 18+
- **下载地址**: https://nodejs.org/
- **安装要点**:
  - ✅ 选择 LTS 版本（推荐）
  - ✅ 默认安装选项即可
  - ✅ 会自动添加到 PATH

### 3. Git
- **下载地址**: https://git-scm.com/
- **或者**: 安装 GitHub Desktop
- **安装要点**:
  - ✅ 默认选项即可
  - ✅ 选择合适的编辑器

## 🔧 可选组件

### FFmpeg（视频处理）
**方法一：直接下载**
- 下载地址: https://www.ffmpeg.org/download.html
- 解压到 `C:\ffmpeg`
- 添加 `C:\ffmpeg\bin` 到系统 PATH

**方法二：使用 Chocolatey**
```powershell
# 先安装 Chocolatey
Set-ExecutionPolicy Bypass -Scope Process -Force; [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# 安装 FFmpeg
choco install ffmpeg
```

## 🎯 验证安装

安装完成后，**重启命令行**，然后运行：

```powershell
# 检查环境
.\环境检查.ps1

# 如果通过，运行完整安装
.\setup_environment.ps1
```

## 📋 完整安装流程

1. **安装基础环境** (Python, Node.js, Git)
2. **重启命令行** (重要！)
3. **运行环境检查**: `.\环境检查.ps1`
4. **运行环境安装**: `.\setup_environment.ps1`
5. **配置 API 密钥** (见下方)
6. **启动项目**: `python 启动_Pervis_PRO.py`

## 🔑 API 密钥配置

### 获取 Gemini API 密钥
1. 访问 [Google AI Studio](https://makersuite.google.com/app/apikey)
2. 创建新的 API 密钥
3. 编辑 `backend/.env` 文件：

```env
GEMINI_API_KEY=your_api_key_here
LLM_PROVIDER=gemini
DATABASE_URL=sqlite:///./pervis_director.db
ASSET_ROOT=./storage/assets
STORAGE_ROOT=./storage
```

## 🚨 常见问题

### 问题1：Python 命令未找到
**解决方案**:
- 重新安装 Python，确保勾选 "Add Python to PATH"
- 或手动添加 Python 到系统 PATH
- 重启命令行

### 问题2：npm 安装失败
**解决方案**:
```powershell
# 清除 npm 缓存
npm cache clean --force

# 使用国内镜像
npm config set registry https://registry.npmmirror.com/
```

### 问题3：权限问题
**解决方案**:
- 以管理员身份运行 PowerShell
- 或修改执行策略：`Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`

### 问题4：网络问题
**解决方案**:
```powershell
# 使用国内 pip 镜像
pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple

# 使用国内 npm 镜像
npm config set registry https://registry.npmmirror.com/
```

## 📞 技术支持

如果遇到其他问题：
1. 检查系统版本兼容性 (Windows 10+)
2. 确保有足够的磁盘空间 (至少 2GB)
3. 临时关闭杀毒软件
4. 检查网络连接

---

**安装完成后，运行 `python 启动_Pervis_PRO.py` 启动项目！**